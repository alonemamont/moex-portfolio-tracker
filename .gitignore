# Personal working files — contain real portfolio data, never commit
*.xlsx
*.xlsm
!template.xlsm

# Personal positions (тикер,коэф,кол-во) — local only
positions.csv

# Local Claude Code settings
.claude/

# Superpowers brainstorming companion mockups (ephemeral, local only)
.superpowers/

# Local isolated development worktrees
.worktrees/
/webapp/src-tauri/target
